entregaveis/
aulas/

# Pesos do modelo (gerenciados pelo DVC)
# models/*.pt
models/*.onnx

# Outputs de inferência
output/
runs/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Imagens de teste grandes
images/

# Video de teste da Aula 7 (grande, so usado localmente pelo yolo_monitor.py)
monitoring/videos/

# Frames brutos/revisados do dataset (evidenciados em prints; não versionados no Git)
dataset/raw/
dataset/annotated/

# Artefatos gerados pelos experimentos de pré-processamento (imagens de inspeção
# visual + datasets temporários usados só para medir mAP) -- não versionar
preprocessing/outputs/

# Variáveis de ambiente locais
.env
